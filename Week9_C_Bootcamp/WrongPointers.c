#include <stdio.h>
struct list {
    int data;
    struct list *next_element;
    };

    int main()
    {
        struct list node;
        // Learn from the error.
        // Why the error.
    }



















